Need to install telingo:

pip instal telingo

And run using

python3 decodew.py final.lp grid1.lp

Or using any other grid. 

To use a customized grid, use:

python3 encodew.py < grid1.txt > grid1.lp

To encode it.
